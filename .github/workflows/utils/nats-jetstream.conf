# Standalone NATS server configuration for CI
port: 4222
http_port: 8222

# Server name (required for JetStream)
server_name: "nats-ci"

# Allow anonymous access for testing
# Authentication and authorization for clients (optional for testing)
# authorization {
#     users = [
#         {
#             user: "ruser"
#             password: "T0pS3cr3t"
#             permissions: {
#                 publish: ">"
#                 subscribe: ">"
#             }
#         }
#     ]
# }

# Enable JetStream
jetstream {
    store_dir: "/tmp/nats"
    # 1GB
    max_mem_store: 1073741824
    # 10GB
    max_file_store: 10737418240
}
